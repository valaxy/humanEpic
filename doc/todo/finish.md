新增一个canvas_view_demo2，不通过反射加载数据，而是使用直接构造的方式传入数据进行渲染
修复ScopePanelDemo，构造演示数据，并且在ScopePanel内部保留哪个Scope被选中。向上抛出被选中事件时戴上scopeName而不是index
我不知道是什么原因导致的，打开canvas_view_demo1一片空白，但是canvas_view_demo2就有内容，我怀疑是摄像机视角的问题，请写一个逻辑每次重新加载Layout的时候都把摄像头重置到内容的中心位置
修复一下UnassignedPoolViewDemo，并且在demo中增加对拖拽能力的测试逻辑
将canvasView，scopePanel，unassignedPoolView目录下的demo相关代码全部移动到flowtool/demo目录下
将flowtool/demo目录下的所有文件全部打平直接放到demo目录下
为CanvasView新增一个鼠标拖动节点的功能，核心逻辑放到一个NodeDraggable的类当中，依赖于CanvasView传递的交互信号来实现，但是CanvasView不依赖于NodeDraggable
为CanvasView新增一个鼠标滚轮滚动可以放大缩小的功能，核心逻辑放到一个MouseWheelZoomable的类当中，依赖于CanvasView传递的交互信号来实现，但是CanvasView不依赖于MouseWheelZoomable
现在可以将scopePanel+canvasView+unassignedPoolView整合到一起了，连接的代码放在 flowtool/Main模块里
	* 左边是scopePanel，中间是canvasView，右边是unassignedPoolView
	* 初始化时scopePanel载入数据，然后选中第一项在canvasView和unassignedPoolView中显示
	* CanvasView中单击删除按钮会将对应的节点删除，但是会在unassignedPoolView中显示
	* 将节点从unassignedPoolView拖入canvasView中会恢复节点和相关边的显示
