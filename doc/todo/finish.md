## 2026-03-13
- √ 节点的删除按钮出现在节点的右上角
- √ canvas区域移除【编辑界面】文本
- √ 右边的未分配池新增一个背景，用来和canvas区域进行视觉区分
- √ flowtool支持节点到自己的自我连线，如果一个方法的名称和参数的名称相同就会出现这种情况
- √ 移除FlowToolVisualNodeDescriptor这种中间结构，纯属多余，用FlowToolMetricNode即可
- √ createDropShadowNode也抽取到FlowToolGraphNodeFactory里面去
- √ FlowToolCanvasPanelController完全可以合入FlowToolCanvasGraphEdit里了
- √ AutosaveControler可以依赖于其他UI组件或者领域层的对象直接完成保存相关的交互和逻辑，不需要FlowToolCanvas来中转逻辑；AutosaveControler应当是一个逻辑处理类，不被其他类依赖，其他类可以通过信号的方式对他抛出事件去处理
- √ 采用一个工厂方法模式，将FlowToolCanvasGraphEdit中关于GraphNode创建的代码抽取到一个独立的文件里
- √ FlowToolCanvasPanelController中有很多代码是不是属于FlowToolCanvasGraphEdit的职责了，考虑一下是否需要将它们抽取到FlowToolCanvasGraphEdit里
- √ 新增一个AutosaveControler，负责监听UI组件的关键行为，并实现自动保存功能，相关逻辑需要从FlowToolDashboard抽取出来
- √ FlowToolDashboard.cs重命名为FlowToolCanvas，并移动到flowtool/canvas目录
- √ 移除unassigned_pool_panel.tscn的StatusLabel，不需要相关的文本状态展示
- √ FlowToolPoolItemButton重命名为UnassignedItem
- √ 可以移除nodeKind了，因为只有一种节点类型，所以节点类型不再有价值了
- √ ScopePanel是一个UI组件，负责对外抛出交互信号
- √ UnassignedPoolPanel将其改造为一个独立的组件，并且有自己独立的tscn文件，相关逻辑从其他文件中抽取
- √ ScopePanel是一个左侧类列表组件，将其改造为一个独立的组件，并且有自己独立的tscn文件，相关逻辑从其他文件中抽取
- √ FlowToolLayoutStore不需要兼容旧版结构的逻辑
- √ 新增一个通用类叫历史数据：专门负责管理按天统计的数据
	- 天用从0开始的整数表示
	- 一个实例只管理一个度量数据，可支持整数或者浮点数
	- 只能对当日数据进行更新，同天的数据通过累加的方式聚合到一起，当日数据一旦更新完毕即封存不能再改
	- 只保留最近N天的数据
	- 提供一个API用来计算最近X天的平均值
	- 对 #AssetItem 进行改造，历史数据那一块的逻辑通过这个新类来实现
- √ 从flowtool中提取领域模型，并将领域模型相关的逻辑放到`flowtool/domain`目录下，顺便调整一下整体逻辑：
	- 指标：每个被`SystemDynamicsProcess`标记的方法都对应一个指标，指标名称就是方法名；方法对应的XML文档中的注释作为指标的中文名称
	- 对应方法中的参数，若存在与参数同名的指标则连接一条参数节点指向指标节点的有向边
	- 移除过程节点这种设计，所有的节点都是指标节点
- √ 默认情况下所有的节点都在未分配池
- √ 节点的标题和内容重复了，可以移除标题，但是要保留删除按钮
- √ 如果节点的中文名存在，那么也要显示
- √ 节点的类型要显示完整
- √ flowtool的领域层不要使用record这总方法来对领域模型进行封装，使用类，并利用面向对象模式对领域层模型进行重构，相关的逻辑从UI组件中抽取过来，但要注意领域层代码不依赖于UI层代码
- √ 彻底重写flowtool的canvas层：
	* 不要使用GraphEdit+GraphNode来实现画布，而是直接自己绘制来实现
	* 每个节点使用矩形表示
	* 节点中间显示节点名称和类型等
	* 节点之间可连线，节点可以连接到自己
	* 其他功能请参考之前的设计来实现
- √ 按规范对flowtool的代码进行一次重构，提高代码可读性
- √ 节点连线若指向自己，则画法是从节点的右侧连出一条线，向右上方走一段距离后再向左上方回到节点的左边

## 2026-03-12
- √ flowtool似乎没有正确的保存，确认每隔一段时间以及在关闭应用程序的时候自动保存一份当前布局配置到config目录，文件格式是JSON
- √ 从未分配池拖动节点到布局界面的时候要出现一个影子节点，代表若当前鼠标释放，节点将会被放置在何处，并且要严格确保鼠标指向哪里，节点就释放在哪里
- √ 在界面左侧新增一个列表，用于显示不同类的布局配置，点击列表项可以切换到不同类对应的布局
- √ 多维度的折线图，并支持显示/隐藏某些维度
- √ 再生成一个示例类，与HelloWorldFlow一起被解析然用用于flowtool工具的测试
- √ flowtool保存功能是将所有类的配置文件保存到一个文件里
- √ flowtool类的列表出现在界面的左侧，明确一下从左到右分别是：类列表、布局界面、未分配池，并且类列表里没有【全部】这种选项
- √ 新增一个针对类的特性标记，只有被标记的类才需要通过反射获取结构关系，因而才会出现在flowtool的列表里
- √ flowtool的布局界面，隐藏重新调整布局的按钮
- √ 节点的节点名称显示在节点内部，不需要一个title显示在节点上方了
- √ 指标节点同时还需要显示出参数的类型，参数类型通过反射在方法上提取
- √ 移除重新提取关系按钮，当关系的双边节点都显示在布局里时，总是显示所有的关系
- √ 选中节点时旁边出现一个删除按钮，点击删除按钮后将节点重新移回未分配池
- √ flowtool的节点高度太大了，改为能刚好容纳文本的高度即可
- √ flowtool的类列表出现在界面的左侧，明确一下左边是类列表，中间是编辑界面、右边是未分配池
- √ 屏蔽flowtool能自定义连线的功能，所有的连线关系都是通过反射从类里提取的，用户无法自定义
- √ FlowToolDashboard实在是太大了，对它进行一次拆分重构，按照UI组件的功能进行拆分，拆分成多个组件，每个组件负责一个功能区域的UI展示和交互逻辑
- √ flowtool出现bug了：连线全部消失了，我只是说连线不能编辑但是仍然需要显示
- √ 隐藏编辑界面上面那个工具栏里的【自动重新调整布局】的按钮

## 2026-03-10
- √ 单元测试框架- √ bug：首次打开界面后默认不会出现连线，需要点击一个节点才会出现连线
